# Program Status

Status artifact: not generated

No canonical project status is available yet. Confirm the project plan through `adp-plan-baseline`, sync actual progress, run `adp-state-audit`, and then run `adp-program-status`.

This starter view is not a fact source and must not be interpreted as `on-plan`, `at-risk`, or `off-plan`.
